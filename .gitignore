# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
.vscode/
*.iml
*.ipr
*.iws
# 루트 고정 — 앵커가 없으면 헥사고날의 adapter/out/ 패키지를 통째로 삼킨다.
/out/

# Java
*.class
*.log
hs_err_pid*

# 로컬 저장소 데이터 — docker-compose 가 여기에 볼륨을 잡는다
docker/*/data/

# 프론트엔드
node_modules/
dist/
.vite/
*.local

# 환경 변수 — API 키는 절대 커밋하지 않는다
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Claude Code - local overrides are per-developer, not shared
.claude/settings.local.json
.claude/sessions/
