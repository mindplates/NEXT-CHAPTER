# 개발은 Windows, CI는 Linux다. 줄바꿈이 저장소 안에서 흔들리지 않게 고정한다.
* text=auto

# 셸 스크립트는 CRLF 로 체크아웃되면 Linux 에서 실행되지 않는다.
*.sh text eol=lf
gradlew text eol=lf

# Windows 전용 스크립트
*.bat text eol=crlf
*.cmd text eol=crlf

# 바이너리
*.jar binary
*.png binary
*.jpg binary
*.svg text
