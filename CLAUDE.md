# NEXT-CHAPTER

## Project Overview

NEXT CHAPTER는 사용자가 고른 주제에 대해 AI가 교육 콘텐츠를 처음부터 끝까지 생성해
제공하는 학습 서비스다. 차별점은 생성 자체가 아니라 **학습자의 반응이 다음에 볼 콘텐츠를
결정한다**는 데 있다.

### 콘텐츠 구조

주제 하나마다 **뼈대(skeleton)** 가 하나 만들어진다.

- 주제가 선택되면 AI가 해당 주제의 전 범위를 한 번에 생성하고 챕터 단위로 분할한다
- 챕터들은 선형 목차가 아니라 **서로 연관 관계를 갖는 그래프**로 연결된다
- 뼈대는 공용이다. 주제 수만큼만 존재하며 사용자 수와 무관하다

사용자는 그래프의 임의 지점에서 진입해 소비하고, 그 과정의 응답·퀴즈 결과·질문이
**사용자 레이어**로 따로 쌓인다.

실제로 화면에 보이는 것은 `뼈대 + 사용자 레이어`를 합성한 결과다. 개인화는 별도 챕터를
덧붙이는 방식이 아니라 공용 뼈대에 대한 변형으로 일어난다. 이 분리 덕분에 축적(뼈대는
주제당 하나)과 개인화(레이어는 사용자당 하나)가 서로 충돌하지 않는다.

#### 레이어가 뼈대를 변형하는 방식

**챕터 본문은 공용 그대로 두고, 그 주변을 개인화한다.**

| 개인화 대상 | 내용 |
|-------------|------|
| 경로 | 그래프에서 다음에 볼 챕터를 그 사람의 이해도·관심에 맞춰 고른다 |
| 보충 블록 | 막힌 지점에 추가 설명·예시를 삽입한다 (본문은 수정하지 않음) |
| 퀴즈 난이도 | 같은 챕터라도 문항 난이도를 조절한다 |

본문 자체를 사용자마다 다시 쓰지 않는 이유는 두 가지다.

1. **집단 신호의 비교 기준이 사라진다** — 여러 사람이 각자 다른 텍스트를 봤다면 "같은 지점에서
   틀렸다"를 비교할 수 없다. 집단 루프는 *동일한 본문에 대한 반응을 겹쳐 보는 것*으로 성립한다.
   본문을 개인화하는 순간 이 제품의 핵심 메커니즘이 작동을 멈춘다.
2. **조회마다 생성 호출이 발생한다** — 캐시가 불가능해지고 비용이 사용자 수 × 조회 수로 늘어난다.

### 콘텐츠와 딜리버리의 분리

**챕터는 한 번 충실하게 생성하고, 전달 형태는 여러 갈래로 뻗는다.**

| 형태 | 내용 |
|------|------|
| 웹 콘텐츠 | 텍스트 + 이미지 + 도표 + 시각화(그래프·인포그래픽). **1차 형태** |
| 영상 강의 | 강의 자료에 음성과 자막을 입힌 동영상 |
| 프레젠테이션 | PPT 등 강의용 자료. 다운로드 또는 발표 모드로 사용 |

클라이언트는 웹으로 시작하되 **모바일·태블릿·PC 앱으로 확장할 수 있게** 설계한다.

#### 소스는 하나다

형태별로 콘텐츠를 따로 생성하지 않는다. 이건 "본문을 사용자마다 다시 쓰지 않는다"와 **같은
논리**다 — 소스가 하나여야 여러 사람의 반응을 겹쳐 볼 수 있고, 소스가 하나여야 여러 형태로
뻗을 수 있다. 형태별로 갈라 생성하면 같은 챕터인데 내용이 달라져 두 이점을 다 잃고, 생성
비용도 형태 수만큼 배가 된다.

그러므로 챕터는 **딜리버리 중립 표현**으로 저장하고, 각 형태는 그것의 렌더링 결과다.

#### 형태는 신호의 교란변수다

딜리버리 형태가 다르면 같은 챕터라도 이해도가 달라진다. 영상이 잘 만들어져 영상 시청자는
잘 맞추고 웹 독자는 틀린다면, 둘을 합친 오답률은 중간값이 되어 **웹 콘텐츠의 결함이 가려진다.**

그래서 **신호에 딜리버리 형태를 함께 기록한다.** 다만 처리 방식은 비대칭이다.

- **임계치 판정은 형태 구분 없이 통합 집계로** 한다. 형태별로 나누면 신호가 형태 수만큼
  쪼개져 초기 규모에서 어느 쪽도 임계치를 못 넘는다 — 주제 통합과 정확히 같은 이유다
- **수정안의 근거로는 형태별 분해를 붙인다.** "이 지점은 웹에서만 오답률이 높다" 같은 진단이
  가능해야 무엇을 고칠지 결정할 수 있다

### 두 개의 피드백 루프

같은 신호를 두 방향으로 쓴다.

| 루프 | 주기 | 신호의 행선지 | 효과 |
|------|------|---------------|------|
| 개인 | 즉시 | 그 사용자의 레이어 | 다음 챕터가 그 사람의 이해도·관심에 맞춰 결정된다 |
| 집단 | 누적 | 공용 뼈대 | 뼈대 자체가 수정되어 이후 모든 사용자가 혜택을 본다 |

집단 루프는 다음 원칙 위에 선다.

> **사용자의 질문은 그 사용자의 부족함이 아니라 콘텐츠의 결함 신고다.**

여러 사람이 같은 지점에서 묻거나 같은 문제를 반복해 틀린다면, 그 챕터가 설명에 실패한
것으로 간주한다. 따라서 별도의 평가 UI가 필요 없다 — 품질 신호는 소비 과정에서 이미
전부 발생한다.

### 대상 사용자

누구나 쓸 수 있는 공개 서비스. 특정 기업·조직 전용이 아니며, 다수 사용자 규모를 상정한다.
조직/테넌트 분리, 학습 배정, 관리자 리포팅 같은 B2B 요구사항은 범위에 없다.

### 필수 제약: 주제 통합

집단 루프는 **신호가 한 뼈대에 모여야** 임계치를 넘는다. 사용자가 자유롭게 주제를 입력하면
"머신러닝 / 기계학습 / ML 입문"이 각각 별도 뼈대를 만들고, 신호가 분산되어 개선이 영원히
트리거되지 않는다. 동시에 생성 비용은 배로 늘고, 콘텐츠 개수는 증가하지만 실제 커버리지는
늘지 않는다.

그러므로 **의미가 같은 주제를 하나의 뼈대로 묶는 것은 부가 기능이 아니라 개선 루프의 전제
조건이다.**

#### 주제 분류 체계: 3단계

주제는 `도메인 > 분야 > 주제` 3단계 카탈로그로 관리한다. **뼈대는 최말단 '주제'에 1:1로
붙는다.**

```
컴퓨터과학              L1 도메인
  └ 인공지능            L2 분야
      └ 머신러닝        L3 주제 = 뼈대 1개
          └ [챕터 그래프]   분류가 아님
```

3단계인 이유는 위아래가 각각 막혀 있어서다.

- **2단계로는 부족하다** — 도메인 하나에 주제가 수백 개 붙는다. LLM에게 "이 입력을 이 목록
  중 하나로" 시킬 때 후보가 많을수록 오매칭이 늘고, 매핑 프롬프트에 카탈로그를 다 넣을 수도
  없다. 중간 층이 있으면 매핑이 층별 좁히기가 되어 각 단계의 후보가 수십 개로 유지된다.
- **4단계는 챕터 그래프와 충돌한다** — 4번째 층("머신러닝 > 지도학습")은 이미 뼈대 안의 챕터
  그래프가 표현하는 층위다. 분류로 한 번 더 쪼개면 뼈대가 갈라지고 신호가 분산된다. 주제
  통합으로 막으려던 바로 그 문제다.

#### 입력 → 뼈대 매핑

사용자는 주제를 자유롭게 입력한다. 카탈로그 고정이 아니다.

1. LLM이 입력을 `도메인 → 분야 → 주제` 순으로 단계적으로 매핑한다
2. 매칭되면 **기존 뼈대에 합류**한다 — "기계학습", "ML 입문", "머신러닝 배우고 싶어"는 모두
   같은 뼈대로 수렴한다
3. 어느 주제에도 매칭되지 않으면 **유사 주제 후보를 제시하고 사용자에게 확인받는다**
4. 사용자가 후보를 모두 거절했을 때만 신규 주제를 카탈로그에 추가하고 뼈대 생성을 시작한다

사용자를 통합의 마지막 판정자로 세우는 것이 핵심이다. 자동 생성으로 흘리면 오매칭 방지망이
없고, 운영자 승인 대기열로 보내면 사용자가 그 자리에서 학습을 시작할 수 없다. 이 확인 단계는
동시에 신규 뼈대 생성 비용의 방어선 역할도 한다.

### 생성 타이밍: 전 범위 일괄, 비동기

주제가 확정되면 AI가 전 범위를 한 번에 생성하되, **비동기 잡으로 돌리고 완료 시 알린다.**
사용자를 로딩 화면에 붙잡아두지 않는다.

**딜리버리 형태도 전부 사전 생성한다.** 웹 콘텐츠·이미지·도표뿐 아니라 영상과 PPT까지 만들어야
published로 넘어간다. 어떤 형태로 진입해도 즉시 이용할 수 있다.

이 선택의 대가는 크고, 감수하기로 한 것이다.

- 첫 소비까지 **수십 분~시간 단위**가 걸린다 (영상 렌더링과 TTS가 지배적)
- 끝까지 소비되지 않을 뼈대에도 전 형태 생성비가 전액 든다
- **집단 루프가 돌 때마다 재생성 비용이 발생한다** — 챕터 하나가 수정되면 그 챕터의 영상과
  PPT를 다시 만들어야 한다. 개선이 잦을수록 비용이 붙는다
- 초기 카탈로그 30~50개를 채우는 비용도 같은 배수로 늘어난다

그럼에도 일괄 생성을 택한 이유는 **뼈대가 항상 완결 상태**라는 점이다. 챕터 그래프 탐색, 집단
신호 집계, 뼈대 수정 루프가 전부 "일부 챕터는 아직 없음" 상태를 다루지 않아도 된다. 부분
생성(lazy)은 이 셋 모두에 분기를 추가한다.

**뼈대당·일일 비용 상한이 여기서 결정적이다.** 파생 자산 없이도 필요했던 안전장치가, 사전
생성을 택하면서 사실상 필수가 됐다.

### 뼈대 수정의 반영

사용자는 **항상 최신본**을 본다. 사용자 레이어는 챕터 ID 기준으로 유지되므로 뼈대가 바뀌어도
개인 학습 기록은 살아남는다.

이미 학습한 챕터가 수정되면 "이 챕터는 개선되었습니다" 배지와 변경 요약을 노출해 재학습을
유도한다. 그 재방문 자체가 개선 효과를 재는 신호가 된다. 이전 버전은 삭제하지 않고 **수정
전후 오답률 비교**를 위해 보관한다 — 3개월 성공 기준 2번 항목이 이 데이터에 의존한다.

#### 파생 자산은 준비된 것 중 최신본을 낸다

웹 콘텐츠는 수정 즉시 최신 버전이 되지만 영상·PPT 재생성에는 시간이 걸린다. 그동안
**이미 생성돼 있는 자산 중 가장 최신 버전을 계속 제공한다.** 형태를 잠그지 않는다.

새 버전 자산이 완성되면 자동으로 교체된다. 즉 **자산은 챕터 버전과 독립적으로 자기 버전을
갖고**, 항상 `완성된 것 중 최댓값`이 서빙된다.

이렇게 하는 이유는 "형태를 일시 중단"이 사용자에게 훨씬 나쁜 경험이기 때문이다. 개선이 잦을수록
영상을 볼 수 없는 시간이 늘어나, 개선 루프가 잘 돌수록 서비스가 나빠지는 역전이 생긴다.

#### 수정 실행 절차

1. 집단 신호가 임계치를 넘으면 AI가 **수정안 + 근거**(어떤 질문·오답이 어느 지점에 얼마나
   모였는지)를 생성해 승인 대기열에 올린다
2. 반영 시점은 운영자 설정을 따른다 — (a) 승인 후 반영, (b) 자동 반영
3. **자동 반영이어도 제안·승인 이력은 전부 남는다.** 자동 반영된 건은 "자동 승인됨"으로
   기록된다. 즉 두 모드는 *반영 시점*만 다르고 감사 추적은 동일하다

이력을 항상 남기는 이유는, AI가 생성한 콘텐츠를 AI가 다시 고치는 구조에서 "언제 무엇이 왜
바뀌었는가"가 유일한 되돌리기 수단이기 때문이다.

#### 트리거 임계치

**낮은 절대치로 시작하고, 수치는 설정값으로 뺀다.** 예: 같은 지점 질문 5건, 또는 시도 20회
이상에서 오답률 40% 초과.

비율 기반이 아닌 이유는 초기 표본이 작아서다. 소비자가 3명일 때 1명만 물어도 33%가 되어
오작동한다. 상대 이상치 탐지(같은 뼈대 안 다른 챕터 대비 통계적으로 튀는 지점)가 정확도는
가장 높지만 비교할 데이터가 쌓이기 전에는 쓸 수 없다 — 데이터가 모이면 그때 도입한다.

임계치는 **3개월 필수 기준을 직격하는 파라미터**다. 시드 사용자가 수십 명일 때도 루프가 한
바퀴 돌 수 있는 값이어야 한다.

### 생성 정확성 검증

단일 수단으로 해결하지 않는다. 상황에 맞게 조합한다.

| 수단 | 적용 시점 |
|------|-----------|
| 출처 근거 생성 | 생성 시 핵심 주장에 출처를 달게 한다 |
| 검증 패스 | 신규 생성 직후, 그리고 뼈대 수정 반영 직전에 주장–출처 일치를 검사 |
| 사용자 오류 신고 | 소비 중 "이 내용 틀렸어요" → 검증 패스 트리거 |
| 집단 신호 | 조용한 오류도 결국 질문·오답 집중으로 드러난다 |

오류 신고 버튼은 "별도 평가 UI가 필요 없다"는 원칙과 충돌하지 않는다. 그 원칙은 *품질 점수를
묻지 않는다*는 뜻이지, 명백한 사실 오류 제보 경로까지 막는다는 뜻이 아니다.

어느 지점에 어느 수단을 얼마나 쓸지(전 챕터 검증 vs 샘플링 등)는 비용을 보고 구현 시 정한다.

### 관리자 콘솔

사용자 경로(주제 입력 → 생성 → 소비)와 **별도로 운영자 경로가 존재한다.** 위에서 흩어져 나온
운영자 기능이 전부 여기로 모인다.

| 영역 | 기능 |
|------|------|
| 카탈로그 | 도메인·분야·주제 관리, 미매핑 입력 검토 |
| 콘텐츠 | 뼈대 **사전 생성**, 검수, 피드백을 통한 수동 개선 |
| 개선 루프 | 수정안 승인 대기열, 자동/수동 반영 모드 전환, 임계치 설정 |
| AI 설정 | 단계별 벤더·모델 지정, API 키 관리 |
| 비용 | 뼈대당·일일 예산 상한 설정 |

주제 입력은 여전히 사용자의 기본 경로다. 운영자의 사전 생성은 그것을 대체하지 않고 **초기
카탈로그를 채우는 수단**이다.

#### 초기 카탈로그 시드

**소수 도메인을 깊게** 채우고 시작한다 — 도메인 1~2개, 분야 5~10개, 주제 30~50개 수준.

넓고 얕게 펴지 않는 이유는 임계치와 같은 문제다. 초기 사용자가 적은데 주제를 넓게 벌리면
뼈대당 사용자가 1~2명씩 붙어 집단 루프가 영원히 임계치를 못 넘는다. 커버리지를 넓히는 것보다
**한 뼈대에 신호를 모으는 것**이 3개월 기준에 직결된다.

> 이 관리자 콘솔은 "B2B 요구사항은 범위에 없다"는 원칙과 무관하다. 조직 분리·학습 배정·관리자
> 리포팅이 아니라, 공개 서비스를 운영하기 위한 **운영자 도구**다.

### 기술 스택

- **백엔드** Spring Boot (Java), **프론트엔드** React — 저장소·툴체인 분리
- **메시지 큐** Kafka — 비동기 생성 잡. 처음부터 도입하는 대신 MVP 운영 부담을 감수한다
- **데이터스토어** PostgreSQL + Neo4j

#### 프론트엔드 구조

React + Vite + TypeScript. **서버 상태는 TanStack Query, 클라이언트 상태는 Zustand**로 나누고
라우팅은 React Router를 쓴다. 디렉터리는 타입이 아니라 기능 기준이다 — `src/features/<도메인>/`,
공용은 `src/shared/`, 앱 조립은 `src/app/`.

상태를 둘로 나누는 이유는, 서버에서 온 값을 클라이언트 스토어에 복사하기 시작하면 그 둘을
맞추는 동기화 코드가 생기기 때문이다. 챕터 블록 문서는 전부 서버 상태이므로 이 경계가 특히
분명하다.

#### 저장소 배치

벡터는 **역할별로 나눠 두고, 같은 벡터를 두 곳에 중복 저장하지 않는다.**

| 저장소 | 담당 |
|--------|------|
| PostgreSQL | 관계형 데이터(주제 메타, 사용자 레이어, 잡, 승인 이력) + **pgvector로 주제 임베딩** |
| Neo4j | 챕터 그래프 + **네이티브 벡터 인덱스로 챕터 임베딩** |

주제 임베딩이 Postgres에 있는 이유는 주제 메타데이터와 같은 트랜잭션에 묶이기 때문이고, 챕터
임베딩이 Neo4j에 있는 이유는 "이 챕터와 유사하면서 그래프상 2단계 이내" 같은 복합 쿼리가 한
번에 되기 때문이다.

**주제 임베딩 컬럼에는 차원을 지정하지 않는다** (`vector`, `vector(1536)`이 아니다). 모델을 관리
화면에서 교체할 수 있는 이상 차원은 고정값이 아니고, 차원을 박으면 모델 교체가 마이그레이션을 요구해
그 결정과 정면으로 부딪힌다. 대가는 pgvector 인덱스(HNSW·IVFFlat)를 만들 수 없다는 점이다 — 인덱스는
차원이 확정된 컬럼만 받는다. 감수하는 이유는 주제 수가 카탈로그 크기에 묶여 있어서다(초기 30~50개).
챕터 임베딩은 뼈대마다 수십 개씩 늘어나 인덱스가 필수고, 그것이 Neo4j 쪽에 있는 또 하나의 이유다.

**벡터 행에는 만든 모델을 함께 저장하고, 검색은 현재 모델로만 비교한다.** 모델을 바꾸면 기존 행이
자동으로 대상에서 빠지므로 별도 무효화 작업이 없고, 재임베딩이 끝나지 않은 주제는 *안 보이는* 쪽으로
실패한다 — 조용히 틀린 결과보다 낫다.

**중복 저장을 피하는 이유는 재임베딩이 실제로 일어나기 때문이다.** 관리자가 임베딩 모델을
교체하면 기존 벡터는 전량 무효가 되고 재임베딩 배치가 돈다. 이 배치는 두 저장소에 원자적으로
걸리지 않으므로, 중복 저장이면 그 사이 한쪽은 구모델·다른 쪽은 신모델 벡터를 갖는다. 모델이
다른 벡터 간 코사인 유사도는 비교 자체가 무의미한데 **에러가 나지 않는다** — 조용히 틀린 주제
매핑이 나오고, 그건 신호를 엉뚱한 뼈대에 붙여 개선 루프의 전제를 무너뜨린다.

#### AI 프로바이더 — 하드코딩하지 않는다

모델은 코드에 박지 않고 **단계별로 관리 화면에서 지정한다.**

- 단계: 뼈대 본문 생성 / 주제 매핑·분류 / 검증 패스 / 개인화 보충 블록 / **임베딩**
- 각 단계마다 **벤더와 모델을 독립적으로 설정**한다. 특정 벤더에 종속되지 않도록 프로바이더
  추상화 계층을 둔다
- API 키도 관리 화면에서 등록·교체한다 (코드·환경변수 하드코딩 금지). DB에는 **AES-GCM
  암호문**으로만 들어가고, 마스터 키만 환경변수에서 온다 — 암호문과 키가 같은 DB에 있으면
  암호화가 의미를 잃는다
- 기본값은 완성 4단계 전부 Claude Opus 5 (`claude-opus-5`). **임베딩만 다른 벤더다** —
  Anthropic은 임베딩 API를 제공하지 않으므로 "전 단계 Claude"를 임베딩에도 적으면 설정은
  그럴듯한데 호출이 실패한다
- 기본값은 코드 상수가 아니라 **마이그레이션이 심는 행**이다. 상수로 두면 설정이 비어 있어도
  호출이 성공해, 관리 화면에서 바꾼 값이 실제로 쓰이는지 확인할 수 없다

**폴백을 두지 않는다.** 단계 설정·벤더 어댑터·API 키 중 하나라도 없으면 호출이 즉시 실패한다.
기본 모델로 조용히 넘어가면 잘못된 모델로 뼈대 하나를 전부 생성한 뒤에야 알게 된다. 같은 이유로
어댑터가 없는 벤더는 **저장 시점에** 거절한다 — 저장은 되고 몇 시간 뒤 생성 잡이 실패하는 쪽이
훨씬 나쁘다.

이 구조의 대가는 임베딩 모델 교체가 **가능해지므로** 재임베딩 경로를 반드시 구현해야 한다는
점이다. 위 저장소 배치 결정이 여기서 나왔다.

#### 생성 비용 상한

**뼈대당 상한과 일일 전체 예산 상한을 이중으로** 둔다. 둘 다 관리 화면 설정값이며 **무제한도
설정할 수 있다.**

- 뼈대당 상한 — 범위가 넘스러운 주제 하나가 예산을 혼자 태우는 것을 막는다
- 일일 전체 예산 — 전체 폭주를 막는다
- 초과 시 생성 중단 + 알림

**상한의 단위는 토큰이다.** 토큰 수는 벤더 응답에서 그대로 오는 정확한 값이지만 단가는 외부에서
바뀌는 사실이다. 단가를 코드나 DB에 박아 두면 벤더가 가격을 바꾼 순간 예산이 조용히 틀려진다 —
과하게 막거나 전혀 막지 못하는데 **어느 쪽도 에러가 나지 않는다.** 운영자가 자기 금액 예산을 현재
단가로 한 번 환산하는 편이 낫다. 단계마다 벤더가 달라 단가 차이가 문제가 되면 그때 단계 설정에
단가 컬럼을 붙인다.

**판정은 호출 전이고, 검사는 AI 게이트웨이 한 곳에만 있다.** 사후 판정만 하면 마지막 호출이 상한을
얼마든지 넘길 수 있고, 사전 생성은 그 한 번이 큰 호출이다. 호출 전에는 실제 소비량을 알 수 없으므로
그 호출의 `maxTokens`를 상한선으로 쓴다. 호출부마다 검사하면 새 경로를 추가할 때 잊는 쪽이
기본값이 되므로, 모든 AI 호출이 지나는 창구에만 둔다.

**뼈대당 상한과 일일 상한을 따로 판정한다.** 주제 매핑처럼 어느 뼈대의 책임도 아닌 호출에는 뼈대당
상한을 적용하지 않고(일일 상한에는 포함한다), 어느 상한에 걸렸는지가 알림 메시지에 필요하다.

**사용량 기록은 생성 트랜잭션과 분리한다.** 생성이 실패해 롤백되더라도 그 호출에 쓴 토큰은 실제로
청구되므로, 사용량이 함께 롤백되면 예산이 조용히 새어 나간다.

**예산 초과는 재시도하지 않는다.** 다른 생성 실패는 재시도가 곧 해결책이지만 상한은 운영자가 올려
주기 전까지 그대로여서 재시도가 같은 결과를 반복하고, 그동안 파티션이 막혀 다른 뼈대의 생성까지
밀린다. 대신 **실패 사유를 반드시 기록한다** — 기록 없이 중단하면 뼈대가 생성 중 상태에 영원히 남고
사용자는 원인을 알 수 없다.

뼈대 생성은 비동기 일괄이므로 **Batch API(전 토큰 50% 할인, 최대 24시간)** 조건에 부합한다.
비동기 생성 결정이 그대로 비용 절반으로 돌아온다. 다만 구현은 비용 코드가 아니라 **생성 파이프라인
③단계의 전송 방식 교체**이고 실패 처리 설계와 맞물리므로, 상한 → 실패 처리 → Batch 순으로 붙인다.

#### ecc 훅

ecc 플러그인 훅은 TypeScript 전제로 짜여 있었다(edit 시 Prettier/tsc). 그 훅들은 **그대로 둔다** —
프론트엔드가 TypeScript다. 대신 포크의 `hooks/hooks.json`에 Java 대응 훅을 **추가**했다.

Gradle을 부르는 포매터·컴파일 훅은 두지 않는다. 편집마다 JVM이 뜨면 편집 자체보다 비용이
커진다. 백엔드 포맷·컴파일은 빌드(Spotless를 `check`에 연결)와 CI가 맡는다.

### 3개월 성공 기준

콘텐츠 개수는 지표로 쓰지 않는다. AI가 생성하므로 버튼을 누르면 늘어나며 변별력이 없다.
증명해야 할 것은 **개선 루프가 실제로 한 바퀴 이상 돌았다는 사실**이다.

- [ ] (필수) 사용자 신호가 집계되어 뼈대 수정을 트리거한 사례가 존재한다
- [ ] 수정 이후 해당 챕터의 오답률·질문 집중도가 실제로 하락했다 — 개선이 개선이었음의 확인
- [ ] 생성된 뼈대 중 끝까지 소비된 비율이 측정되고 있다

첫 항목만 달성해도 제품의 핵심 가설은 증명된다.

### 아직 정하지 않은 것

제품 방향과 기술 스택은 위에서 확정됐다. 남은 것은 구현 단계에서 측정해야 정해지는 값들이다.

- **임계치·상한의 실제 수치** — 구조는 정했고 설정값으로 뺐다. 초기 사용자 규모에서 루프가
  한 바퀴 도는 값이 얼마인지는 돌려 봐야 안다
- **검증 패스의 적용 범위** — 전 챕터에 돌릴지 샘플링할지. 뼈대당 실제 생성 단가를 재고
  결정한다
- **구버전 자산 안내 표시 여부** — 서빙 정책은 정해졌으나(준비된 것 중 최신본), 사용자에게
  "이 영상은 이전 버전 기준입니다"를 알릴지는 미정
- **블록 ID 안정성 유지 방식** — 수정 시 기존 블록 ID를 어떻게 이어 붙일 것인가. ID가 전부
  갈리면 수정 전후 신호를 연결할 수 없다
- **오브젝트 스토리지·TTS·영상 렌더 수단** — 자체 운영 / 클라우드 서비스
- **딜리버리 형태별 우선순위** — 초기에 웹만 만들고 영상·PPT를 뒤로 미룰지, 처음부터 전부
  만들지. 사전 생성을 택했으므로 이 선택이 초기 비용을 좌우한다
- **Kafka·Neo4j 운영 형태** — 자체 운영 / 매니지드 서비스
- **소셜 로그인 제공자** — 어느 제공자를 지원할 것인가
- **관리자 역할 세분화** — `ADMIN` 단일 역할로 충분한가, 검수자·운영자를 나눌 것인가
- **Neo4j 재구축 절차** — 파생 저장소이므로 통째 재생성이 가능해야 한다. outbox 재생 방식인지
  Postgres 전량 스캔인지 미정
> 생성 실패·부분 실패 처리는 Architecture의 생성 파이프라인 절에서 해결됐다
> (전 챕터 완료 시에만 published, 챕터 단위 재시도 후 운영자 큐).

## Architecture

Project Overview가 "무엇을·왜"라면 이 절은 "어떻게"다.

### 모듈 구조

헥사고날 아키텍처, **계층축 멀티모듈**. 모듈·패키지 관례는 사내 **WORKS 프로젝트
(`../WORKS`)를 그대로 따른다** — 새 규칙을 만들지 않는다.

```
nextchapter-common
nextchapter-domain
nextchapter-application
nextchapter-adapter/
  web            REST                    (in)
  messaging      Kafka                   (in/out)
  persistence    PostgreSQL              (out)
  graph          Neo4j                   (out)
  cache          Redis                   (out)
  storage        오브젝트 스토리지        (out)
  ai             LLM 프로바이더           (out)
  media          TTS · 영상 렌더 · PPT    (out)
  security
nextchapter-bootstrap
```

| 모듈 | 패키지 규칙 |
|------|-------------|
| domain | `com.mindplates.nextchapter.domain.{도메인}.{model,exception}` |
| application | `...application.{도메인}.{service, view, port.in, port.out, port.in.command}` |
| adapter (in) | `...adapter.in.web.{도메인}` |
| adapter (out) | `...adapter.out.persistence.{도메인}`, `...adapter.out.graph.{도메인}` |
| common | `...common.{exception, http, response, time, ...}` |
| bootstrap | `com.mindplates.nextchapter.{, config}` |

**도메인 축은 패키지로만 구분한다** — catalog / skeleton / chapter / layer / signal /
generation / admin. 도메인 경계가 아직 검증되지 않았으므로 모듈로 굳히지 않는다.

**어댑터는 외부 시스템별로 모듈을 나눈다.** WORKS가 jira·github·figma를 각각 모듈로 둔 것과
같은 이유로, 여기서는 저장소·메시징·AI 프로바이더가 각각 모듈이 된다. AI 어댑터를 독립 모듈로
두는 것은 **벤더 교체 가능성**(관리 화면에서 단계별 벤더 지정)의 구조적 대응이기도 하다.

### 뼈대 생성 파이프라인

4단계, **단계별 Kafka 토픽 체인**.

```
주제 확정
   │
   ▼  skeleton.graph.requested
①  그래프 생성      챕터 제목·요약·관계
   │
   ▼  skeleton.outline.requested
②  개요 생성        챕터별 개요 — 중복·누락을 여기서 잡는다
   │
   ▼  skeleton.body.requested    ← 챕터 수만큼 발행 (파티션으로 병렬도 조절)
③  본문 생성        블록 문서. 챕터별 독립 재시도
   │
   ▼  skeleton.asset.requested   ← 챕터 × 형태만큼 발행
④  파생 자산 생성    이미지·도표 → 영상(TTS+렌더) → PPT
   │
   ▼  전 챕터 · 전 형태 성공 시에만
published
```

상태: `generating_graph → generating_outlines → generating_bodies → generating_assets → published`

- **전부 완료해야 공개한다.** 일괄 생성의 근거가 "뼈대는 항상 완결 상태"이므로, 부분 공개는
  그래프 탐색·집계·수정 루프 **전부에** 미완성 분기를 만든다
- 실패한 항목은 N회 자동 재시도 후 운영자 큐로 보낸다. 뼈대는 published로 넘어가지 않는다
- 개요 단계를 둔 대가는 호출 횟수와 대기시간 증가다. 그걸 감수하는 이유는 챕터 간 중복·누락을
  **본문 생성 전에** 잡는 편이 훨씬 싸기 때문이다
- ④가 전체 소요시간을 지배한다. 영상은 TTS와 렌더링이 붙어 다른 단계와 자릿수가 다르므로,
  **형태별로 토픽·파티션을 분리해** 무거운 형태가 가벼운 형태를 막지 않게 한다

#### 토픽 네이밍과 파티션 키

토픽 이름은 `{집합체}.{단계}.{이벤트}`(소문자·점 구분)이고, 코드 상수가 아니라 설정값이다 —
`prefix`를 붙일 수 있어야 한 클러스터를 공유할 때 개발과 스테이징의 토픽이 섞이지 않는다.

| 토픽 | 키 | 파티션 |
|------|-----|--------|
| `skeleton.graph.requested` | 뼈대 ID | 3 |
| `skeleton.outline.requested` | 뼈대 ID | 3 |
| `skeleton.body.requested` | **챕터 ID** | 12 |

**본문 토픽의 키가 챕터 ID인 것이 핵심이다.** 뼈대 ID를 키로 쓰면 한 뼈대의 챕터가 전부 같은 파티션에
몰려 병렬도가 1이 되고, 파티션을 늘려도 효과가 없다. 그 실패는 에러 없이 "그냥 느린 것"으로 보인다.
키를 비우면 병렬도는 나오지만 같은 챕터의 재시도가 다른 파티션에 갈 수 있다.

**토픽은 코드로 선언한다** — 브로커 자동 생성에 맡기면 파티션이 기본값(보통 1)이 된다.

**메시지 페이로드는 문자열 JSON이고 타입 헤더를 쓰지 않는다.** 타입 헤더에는 Java 클래스 이름이
박혀서 클래스를 옮기는 것만으로 토픽에 쌓인 옛 메시지가 죽는다. 같은 이유로 도메인 레코드를 직접
직렬화하지 않고 와이어 전용 레코드를 지난다.

**상태 전이는 한 칸씩 전진만 한다.** 건너뛰면 개요 없이 본문을 쓰게 되고 그 실패는 생성이 끝난 뒤에야
드러난다. Kafka는 at-least-once이므로 같은 완료 메시지를 두 번 받는 것은 정상이고, 그때는 전이를
건너뛴다. 상태 전이와 다음 이벤트 발행은 한 곳을 지난다 — 나뉘면 상태만 올라가고 아무도 다음 단계를
시작하지 않거나, 이벤트만 나가고 진행률이 거짓이 된다.

**뼈대 수정 시에도 ④를 다시 돈다.** 챕터 본문이 바뀌면 그 챕터의 파생 자산이 전부 무효가 되기
때문이다. 수정 반영은 곧 재생성 잡의 트리거다.

### 챕터 표현: 블록 문서

챕터 본문은 **딜리버리 중립 블록 문서**다. 블록마다 타입과 **안정적인 ID**를 갖는다.

```
chapter
 ├ b1  heading     "경사하강법이란"
 ├ b2  paragraph   "손실함수를 ..."
 ├ b3  figure      { type: plot, spec: {...} }
 ├ b4  formula     "\theta := \theta - ..."
 ├ b5  narration   "이제 학습률을 ..."      ← 음성·자막 원천
 └ b6  quiz        {...}
```

렌더러가 형태별로 해석한다 — 웹은 HTML, 영상은 `narration`을 TTS·자막으로, PPT는 슬라이드로.
`narration`처럼 **특정 형태에서만 쓰이는 블록도 같은 문서 안에 둔다.** 형태별로 문서를 나누는
순간 소스가 갈라지기 때문이다.

**블록 ID가 이 설계의 핵심 이득이다.** 질문·오답 신호를 챕터가 아니라 **블록 단위로** 붙일 수
있어, "여러 사람이 같은 지점에서 묻는다"의 *지점*이 정밀해진다. 챕터 단위 신호로는 "이 챕터가
어렵다"까지만 알 수 있지만, 블록 단위면 어느 문단·어느 수식에서 막히는지가 나온다.

블록 ID는 버전이 올라가도 **가능한 한 유지한다.** 수정으로 ID가 전부 갈리면 수정 전후 신호를
이어 볼 수 없어 3개월 성공 기준 2번(개선이 개선이었는지 확인)이 깨진다.

#### 블록 ID 규칙

형식은 `b{순번}`이고, 발급은 **챕터에 저장된 단조 증가 카운터**가 한다. 수정 시에는 프롬프트에 기존
문서를 ID와 함께 넣어 유지되는 블록이 같은 ID를 쓰게 하고, 돌려받은 ID가 기존 집합에 없거나 형식이
틀리거나 문서 안에서 중복이면 **버리고 새로 발급한다.**

지켜야 하는 것이 둘인데 **두 번째가 더 위험하다.**

1. **유지** — 갈리면 수정 전후 신호를 이어 볼 수 없다
2. **재사용 금지** — v1의 `b3`가 v5에서 다른 내용이 되면 두 버전의 신호가 한 블록에 섞인다. 이쪽은
   *에러 없이* 잘못된 결론을 만든다

그래서 카운터를 문서에서 계산하지 않고 챕터에 저장한다. 문서의 최대 순번으로 다음 값을 정하면 블록을
지웠다가 추가할 때 지워진 ID가 되살아난다. 잘못된 ID를 통과시키는 것보다 신호가 새 블록에서 다시
시작하는 편이 낫다 — 전자는 다른 블록의 신호를 오염시키고, 후자는 그 블록의 신호만 끊는다.

**본문 기록 경로는 하나다.** 최초 생성과 이후 수정(집단 루프·검수·검증 패스)이 같은 유스케이스를
지난다. 나누면 ID 승계와 버전 증가 규칙이 두 곳에 복제되고, 한쪽만 고치는 순간 조용히 어긋난다.

### 저장소 경계

| 저장소 | 담당 | 성격 |
|--------|------|------|
| PostgreSQL | 챕터 블록 문서·메타·버전, 주제 카탈로그, 사용자 레이어, 잡, 승인 이력, 주제 임베딩(pgvector) | **진실의 원천** |
| 오브젝트 스토리지 | 파생 자산 바이너리 (이미지·영상·PPT) | 파생 |
| Neo4j | 챕터 ID·관계·챕터 임베딩 | 파생 |
| Redis | 블록 문서 캐시 | 파생 |
| Kafka | 생성 잡 체인, outbox 발행, 신호 이벤트 | 전달 |

영상·PPT 바이너리는 DB에 넣지 않는다. Postgres에는 **자산 메타데이터**(형태, 버전, 스토리지
키, 생성 상태)만 두고 실체는 오브젝트 스토리지에 있다. 자산도 파생물이므로 블록 문서에서
언제든 다시 만들 수 있다.

**챕터 본문의 원천은 Postgres다.** Neo4j는 ID·관계·임베딩만 갖는 얇은 그래프다. 수정 이력과
승인 기록이 본문과 같은 트랜잭션에 묶여야 하고, 그래프가 가벼울수록 탐색이 빠르다. 대가는
탐색 후 본문 조회가 2홉이라는 점이다.

#### 스키마의 원천은 마이그레이션 SQL이다

Postgres 스키마는 **Flyway 마이그레이션**(`nextchapter-adapter/persistence/.../db/migration`)이 만들고,
JPA는 `ddl-auto=none`으로 스키마를 건드리지 않는다. 스키마를 소유하는 모듈이 스키마 파일도 갖는다.

엔티티가 스키마를 만들게 두지 않는 이유는 두 가지다.

1. **컬럼 타입·인덱스·제약이 리뷰에 보이지 않는다** — 실행해 봐야 아는 스키마가 된다
2. **표현되지 않는 것이 있다** — `CREATE EXTENSION vector`, pgvector 인덱스, 부분 인덱스는 엔티티
   애노테이션으로 나오지 않는다

이 선택의 대가는 컬럼을 추가할 때 파일 두 곳(엔티티와 마이그레이션)을 고쳐야 한다는 점이다.

**제약은 애플리케이션이 아니라 DB에 둔다.** `skeletons.topic_id`의 UNIQUE가 "뼈대는 주제에 1:1"을
지키는 실제 수단이다 — 애플리케이션 검사만 두면 동시 요청 두 건이 같은 주제로 뼈대를 두 개 만들고,
그 순간 집단 신호가 반으로 갈린다.

#### 이중 쓰기: Transactional Outbox

Postgres와 Neo4j에 걸친 쓰기에는 **분산 트랜잭션이 없다.** 한쪽만 성공하면 그래프에 없는
챕터, 또는 본문 없는 노드가 생긴다. 순차 쓰기 + 보상 방식은 보상 자체가 실패할 수 있고 그
사이 프로세스가 죽으면 불일치가 남는다.

```
┌─ Postgres 트랜잭션 ──────────┐
│  본문·버전  INSERT           │
│  outbox    INSERT            │  ← 같은 커밋
└──────────────┬───────────────┘
               ▼  퍼블리셔
            Kafka
               ▼
      Neo4j 어댑터 (멱등)
```

챕터 ID + 버전을 멱등 키로 쓰므로 재시도가 안전하다. 최종 일관성이지만 **유실은 없다.**
Kafka 도입이 여기서 두 번째 정당성을 얻는다.

### 합성과 렌더의 경계

두 가지를 구분한다. 섞으면 앱 확장 때 걸린다.

| | 주체 | 산출물 |
|---|------|--------|
| **합성** | 서버 | `뼈대 블록 문서 + 사용자 레이어` → 개인화된 블록 문서 |
| **렌더** | 클라이언트 | 블록 문서 → 화면 (웹/iOS/Android가 각자) |

"백엔드 합성"은 **레이어 병합**을 뜻하지 HTML 생성을 뜻하지 않는다. 서버는 어느 플랫폼에서도
쓸 수 있는 블록 문서를 내리고, 렌더링은 클라이언트가 한다. 새 플랫폼이 생겨도 **API는 그대로다.**

영상·PPT는 예외다 — 서버가 미리 만들어 오브젝트 스토리지에 둔 산출물이므로 클라이언트는 URL만
받는다.

### 캐시

**캐시 키에 버전과 형태를 포함한다** — `chapter:{id}:v{version}:{format}`. 뼈대가 수정되면 버전이
올라가 새 키를 보게 되므로 **무효화 로직 자체가 필요 없다.** 구버전 키는 TTL로 자연 만료된다.
챕터 버전을 전체 스냅샷으로 저장하기로 했으므로 버전 번호는 공짜로 생긴다.

#### 자산 버전은 챕터 버전을 뒤따른다

블록 문서는 항상 최신 챕터 버전으로 서빙되지만, 파생 자산은 **생성이 완료된 것 중 최신
버전**으로 서빙된다. 자산 메타에 자신의 기준 `chapterVersion`을 갖고, 조회 시 그중 최댓값을
고른다.

```
챕터 v5 (블록 문서)          ← 웹은 항상 여기
  ├ 자산 web-figures  v5     ✅ 완료
  ├ 자산 video        v4     ✅ 완료  ← 지금 서빙되는 것
  │        video      v5     ⏳ 생성 중
  └ 자산 pptx         v5     ✅ 완료
```

`v5` 영상이 완성되면 조회 결과가 자동으로 넘어간다. 별도 전환 작업이 없다.

명시적 무효화를 쓰지 않는 이유는, 삭제 호출이 한 번만 누락돼도 사용자가 구버전을 계속 보게
되고 그게 **에러 없이 조용히 틀리기** 때문이다. "개선되었습니다" 배지와 실제 본문이 어긋나는
상태가 정확히 그 형태다.

### 챕터 버저닝

수정될 때마다 `chapter_versions`에 본문 전문을 **스냅샷**으로 쌓는다. 델타(diff) 저장이 아닌
이유는 세 가지다.

1. 승인 이력 요구사항("언제 무엇이 왜 바뀌었나")이 diff 재구성이 아니라 단순 조회로 끝나야 한다
2. 3개월 성공 기준 2번(수정 전후 오답률 비교)이 임의 시점의 본문을 필요로 한다
3. 챕터 본문은 수 KB라 스냅샷 저장 비용이 사실상 무시할 만하다

### 신호 경로

주기가 다른 두 루프를 한 경로로 처리하지 않는다.

| 루프 | 경로 |
|------|------|
| 개인 (즉시) | Postgres 동기 기록 → 다음 챕터 결정 시 곧바로 읽음 |
| 집단 (누적) | Kafka 발행 → 집계 컨슈머 → 집계 테이블 → 임계치 검사 |

같은 신호를 양쪽에 보내는 것은 Overview의 "같은 신호를 두 방향으로 쓴다"를 그대로 구현한
것이다. 전부 Kafka로만 흘리면 개인 루프가 최종 일관성이 되어 "방금 답한 결과가 다음 챕터에
반영되지 않는" 타이밍 문제가 생긴다.

신호 레코드는 **블록 ID와 딜리버리 형태를 함께 담는다.**

```
{ chapterId, chapterVersion, blockId, format, type, payload, userId, at }
```

- `blockId` — "같은 지점에서 묻는다"의 지점을 문단·수식 단위로 특정한다
- `format` — 임계치 판정에는 쓰지 않고(형태별로 나누면 신호가 쪼개진다), 수정안의 근거로
  형태별 분해를 만들 때 쓴다
- `chapterVersion` — 수정 전후 비교의 기준선이다. **사용자가 실제로 소비한 버전**을 기록한다.
  웹은 최신 챕터 버전이지만 영상은 자산 버전이 뒤처져 있을 수 있으므로, 무조건 최신 버전으로
  기록하면 구버전 영상을 본 사람의 오답이 최신 버전의 실패로 잘못 집계된다

### API

**REST.** 서버는 챕터 단위로 합성된 블록 문서(와 자산 URL)를 내린다. 캐시 키가 URL로 떨어져
단순하고, 클라이언트가 웹이든 앱이든 같은 엔드포인트를 쓴다.

GraphQL은 쓰지 않는다. *콘텐츠가 그래프인 것*과 *API가 그래프 쿼리여야 하는 것*은 별개이며,
클라이언트가 쿼리 모양을 정하면 서버 캐시 키가 불안정해져 위의 버전 키 전략과 충돌한다.

### 인증·인가

**사용자와 관리자의 인증 경로를 분리한다.**

| 대상 | 방식 |
|------|------|
| 일반 사용자 | OAuth2 소셜 로그인 → JWT |
| 관리자 | 자체 계정 → JWT, 역할 `ADMIN` |

사용자 쪽을 소셜로 두는 이유는 공개 서비스라 진입장벽을 낮춰야 하고, 비밀번호 저장·재설정·
유출 대응을 떠안지 않기 때문이다. 관리자 쪽을 자체 계정으로 분리하는 이유는 **운영자 접근이
외부 제공자 장애에 종속되면 안 되기 때문이다** — 소셜 제공자가 죽었을 때 승인 대기열과 비용
설정에 접근할 수 없는 상태는 받아들일 수 없다.

대가는 인증 경로가 두 개라는 점이다. `adapter/security` 모듈이 이 둘을 모두 담당한다.

경로 분리는 **필터 체인 두 개**로 구현한다. 하나에 규칙을 섞으면 사용자 쪽 설정을 고칠 때 관리자
경로가 함께 흔들려, 분리하려던 이유가 사라진다.

- 관리자 체인은 **무상태 Bearer 토큰**이라 쿠키를 쓰지 않는다. 브라우저가 자동 전송하는 자격 증명이
  없으므로 CSRF가 성립하지 않아 이 체인에서만 끈다. 사용자 체인은 쿠키를 쓸 수 있어 켜 둔다
- 최초 관리자 계정은 **계정이 0개일 때만** 설정값으로 만든다. 관리자 계정은 관리 화면에서 만들고
  관리 화면은 관리자 인증을 통과해야 열리므로 어딘가에서 순환을 한 번 끊어야 하고, "0개일 때"가
  가장 작은 구멍이다. 조건이 없으면 설정 파일의 비밀번호가 상시 백도어가 된다
- JWT 서명 키에 **기본값을 두지 않는다.** 없으면 부팅마다 임의 키를 쓰고 경고한다 — 재기동 시
  토큰이 무효가 되므로 운영에서는 반드시 설정하게 된다. 고정 기본키를 두면 아무 증상 없이 배포본
  전체가 같은 키를 쓰는 상태가 되고, 그건 조용히 틀린다

### 테스트 전략

계층별로 테스트 종류를 매핑한다. 프로젝트 규칙의 단위/통합/E2E 3종이 그대로 대응된다.

| 계층 | 테스트 |
|------|--------|
| domain | 순수 단위 테스트 (의존 없음) |
| application | **out 포트를 목킹**한 단위 테스트 |
| adapter/persistence | Testcontainers — 실제 PostgreSQL |
| adapter/graph | Testcontainers — 실제 Neo4j |
| adapter/messaging | Testcontainers — 실제 Kafka |
| adapter/ai | LLM 응답 스터브 |
| adapter/web | MockMvc |
| 핵심 사용자 플로우 | E2E (Playwright) |

**어댑터를 실제 저장소에 붙여 검증하는 것이 핵심이다.** 이 제품의 위험은 도메인 로직이 아니라
저장소 경계에 몰려 있다 — outbox, 벡터 검색, 그래프 쿼리, 버전 키 캐시. 그 구간을 목으로
덮으면 정작 깨질 곳이 테스트되지 않는다.

반대로 application 계층까지 실제 저장소를 띄우면 TDD 리듬이 무너진다. 그래서 로직은 목킹으로
빠르게, 매핑·쿼리는 실제로 검증한다.

### 로컬 개발 환경

`docker-compose` 파일 하나로 저장소 5개를 전부 띄운다 (`docker compose up`). 버전이 코드와 함께
버전 관리된다.

**로컬 포트는 34000번대를 100 단위로 배정한다.** 애플리케이션부터 시작해 저장소 순으로 채운다.

| 서비스 | 기본 포트 | 로컬 포트 |
|--------|-----------|-----------|
| 백엔드 | 8080 | **34000** |
| 프론트엔드 (dev) | 5173 | **34100** |
| PostgreSQL (pgvector) | 5432 | **34200** |
| Neo4j (Bolt) | 7687 | **34300** |
| Neo4j (HTTP) | 7474 | **34400** |
| Kafka | 9092 | **34500** |
| Redis | 6379 | **34600** |
| 오브젝트 스토리지 (MinIO 등) | 9000 | **34700** |

기본 포트를 그대로 쓰지 않는 이유는 개발 머신에 이미 떠 있는 다른 프로젝트의 인스턴스와
충돌하기 때문이다. 100 단위로 띄워두면 서비스 하나가 부가 포트(디버그, 메트릭, 관리 콘솔 등)를
필요로 할 때 자기 구간 안에서 해결할 수 있다 — 예: 백엔드 디버그 포트는 34001.

새 서비스는 다음 100 단위(34800, 34900 …)를 이어 쓴다.

Testcontainers는 랜덤 포트를 쓰므로 이 표와 무관하다 — 위 포트는 **개발용 상시 환경 전용**이다.

### 구현 순서

**골격과 CI를 먼저 세운다.** 첫 단계는 기능 없이 다음만 만든다.

- Gradle 멀티모듈 골격 + 패키지 구조 (WORKS 관례)
- `docker-compose` (위 포트 표)
- CI 파이프라인 (빌드·테스트·커버리지)
- 헬스체크 엔드포인트

기능 없는 단계를 하나 쓰는 대신, 이후 모든 작업이 같은 바닥 위에서 시작한다. 수직 슬라이스는
그다음이다.

### 운영 대상

PostgreSQL · Neo4j · Kafka · Redis · 오브젝트 스토리지 — **5개**. MVP 단계 인프라로 작지 않은
규모다. 각각의 도입 근거는 위에 있으나, 운영 부담을 감수한 선택이라는 점을 기록해둔다.

외부 유료 의존도 늘어난다 — LLM 프로바이더에 더해 **TTS와 영상 렌더링**이 붙는다. 전 형태
사전 생성을 택했으므로 이 둘이 생성 비용의 큰 몫을 차지한다.

## 구현 계획

**언제·어떤 순서로 만들 것인가는 [`docs/`](docs/)에 있다.** 이 문서는 *무엇을·왜*(Project
Overview)와 *어떻게*(Architecture)까지만 다루고, 일정과 진행 상태는 그쪽에서 관리한다.

- [`docs/README.md`](docs/README.md) — 문서 인덱스 + 진행 대시보드
- [`docs/roadmap/`](docs/roadmap/) — 마일스톤 M0~M7, 의존 관계, 크리티컬 패스

로드맵은 3개월 성공 기준의 **필수 항목**(신호가 뼈대 수정을 트리거한 사례)에서 역산한 순서다.
크리티컬 패스는 `M0 기반 → M1 카탈로그 → M2 생성 → M3 소비 → M5 집단 루프`이며, 개인 루프(M4)와
딜리버리 확장(M6)은 그 밖에 있다.

위 "아직 정하지 않은 것"의 각 항목은 **정산해야 하는 마일스톤에 배치돼 있다.** 결정이 확정되면
로드맵이 아니라 이 문서로 옮긴다 — 확정된 결정이 로드맵에만 남아 있으면 설계 문서가 거짓이 된다.

## Rules

These are always-follow guidelines. Read them before writing code.

@.claude/rules/security.md
@.claude/rules/coding-style.md
@.claude/rules/testing.md
@.claude/rules/git-workflow.md
@.claude/rules/patterns.md
@.claude/rules/performance.md
@.claude/rules/agents.md
@.claude/rules/hooks.md

## Tooling

This project uses the [everything-claude-code](https://github.com/mindplates/everything-claude-code)
plugin, declared in `.claude/settings.json`. That repo is our fork of
[worldflowai/everything-claude-code](https://github.com/worldflowai/everything-claude-code);
we fork rather than consume upstream directly because Claude Code merges hooks across sources
and offers no way to disable an individual one, so removing a hook means editing the plugin
source. To pull upstream changes:

```bash
git remote add upstream https://github.com/worldflowai/everything-claude-code.git
git fetch upstream && git merge upstream/main
```

It provides:

- **Commands**: `/plan`, `/tdd`, `/verify`, `/code-review`, `/build-fix`, `/refactor-clean`,
  `/e2e`, `/learn`, `/checkpoint`, `/orchestrate`, `/test-coverage`, `/setup-pm`, `/eval`,
  `/update-docs`, `/update-codemaps`
- **Agents**: planner, architect, tdd-guide, code-reviewer, security-reviewer,
  build-error-resolver, e2e-runner, refactor-cleaner, doc-updater
- **Skills**: coding-standards, backend-patterns, frontend-patterns, tdd-workflow,
  security-review, verification-loop, eval-harness, continuous-learning, strategic-compact
- **Hooks**: session memory persistence, strategic compaction, console.log warnings,
  Prettier/tsc on edit, PR URL logging

### Typical feature flow

```
/plan          -> plan the approach
/tdd           -> tests first, then implementation
/verify        -> validate the change works
/code-review   -> self-review before PR
```

### Hook notes

Hooks are enabled as shipped by the fork, with one upstream hook removed: the one that
blocked `npm run dev` unless it ran inside tmux, which does not exist on Windows.

Still active and worth knowing about:

- Creating any `.md` file other than README/CLAUDE/AGENTS/CONTRIBUTING is **blocked**.
- A non-blocking "consider running in tmux" reminder prints on `npm install`/`test`,
  `cargo build`, `docker`, `pytest`, and similar. Harmless noise on Windows.

There is no way to disable a single hook from settings — hooks merge across sources and
only `disableAllHooks` (all-or-nothing) exists. To drop another one, edit `hooks/hooks.json`
in the fork and push.

**Stack fit.** The edit-time hooks assume TypeScript (Prettier, `tsc`). Those stay — the
frontend is TypeScript. What was added is the Java **counterpart**, not a replacement:
a `System.out`/`System.err`/`printStackTrace` warning on `.java` edits, mirroring the
`console.log` hook ([fork PR #1](https://github.com/mindplates/everything-claude-code/pull/1),
awaiting merge).

**No Gradle hook on edit.** Formatting and compilation for the backend run in the build, not
per edit — Spotless is wired into `check`, and CI runs `./gradlew build`. Invoking Gradle on
every edit costs seconds per edit; a JVM start dwarfs the edit itself.
